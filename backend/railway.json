{
      "$schema": "https://railway.app/railway.schema.json",
      "build": {
              "builder": "NIXPACKS"
      },
      "deploy": {
              "startCommand": "php artisan tinker --execute=\"DB::statement('SET FOREIGN_KEY_CHECKS=0'); Artisan::call('migrate:fresh', ['--force' => true]);\" && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan storage:link && php artisan serve --host=0.0.0.0 --port=$PORT",
              "healthcheckPath": "/api/health",
              "restartPolicyType": "ON_FAILURE"
      }
}
